# Greetings

## Login Greeting (System Overview)

After logging into your VPS via SSH, KiwiPanel displays a concise **system greeting**
that provides immediate visibility into the server’s current state.

This greeting is designed to give administrators **useful information at a glance**
without requiring any additional commands.

---

### What You See After Login

Upon successful login, KiwiPanel prints a structured system summary similar to:

![An image](/static/greeting.png)

As suggested, the greeting includes some general information about your currrent VPS. KiwiPanel CLI has a bundle of commands that can be run by typing `kiwipanel command` such as `kiwipanel check` or `kiwipanel status`. Simply type `kiwipanel` to see the whole list.
